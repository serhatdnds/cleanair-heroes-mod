package com.cleanairheroes.common.blocks;

import net.minecraft.world.level.block.Block;

public class BicyclePathBlock extends Block {
    public BicyclePathBlock(Properties properties) {
        super(properties);
    }

    // TODO: Add bicycle path functionality
    // This block will provide speed boost for players walking on it
}